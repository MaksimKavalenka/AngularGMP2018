import { ReleaseBorderDirective } from './release-border.directive';

describe('ReleaseBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new ReleaseBorderDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
