import { MinAuthorsDirective } from './min-authors.directive';

describe('MinAuthorsDirective', () => {
  it('should create an instance', () => {
    const directive = new MinAuthorsDirective();
    expect(directive).toBeTruthy();
  });
});
