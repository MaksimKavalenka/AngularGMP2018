import { DateRegexpDirective } from './date-regexp.directive';

describe('RegexpDirective', () => {
  it('should create an instance', () => {
    const directive = new DateRegexpDirective();
    expect(directive).toBeTruthy();
  });
});
